<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
         <div class="form p-10 text-white">
        <form action="/create" method="post">
         <input  class="block w-full rounded-md px-5 py-2 bg-zinc-800" name="title" placeholder="Enter the title"/>
         <textarea class="block w-full rounded-md mt-2 resize-none px-5 py-2 bg-zinc-800" name="description" placeholder="Enter the descriptiion"></textarea>
            <input class="mt-2 bg-blue-600 rounded-md px-5 py-3 cursor-pointer" type="submit" value="Create task"/>
        </form>
     </div>
     <div class="task p-10 flex flex-wrap gap-3">
        <% if(files?.length > 0) { %>
        <% files.forEach(function (item){ %>
            <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                <h1 class="text-white text-2xl tracking-tighter"><%= item %></h1>
                  <a class="text-blue-500 inline-block mt-3" href="">Read more</a>
        </div>
        <% })%>
       <% } else { %>
        <h3 class="text-zinc-600">No tasks</h3>
      <% } %>
     </div>
    </div>
    
    
    <script src="/js/index.js"></script>
</body>
</html>